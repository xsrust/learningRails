<%= form_for(@token_access_rite) do |f| %>
  <% if @token_access_rite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@token_access_rite.errors.count, "error") %> prohibited this token_access_rite from being saved:</h2>

      <ul>
      <% @token_access_rite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
